name: kubadilishanaapp
description: "Kubadilishana - Waliopangana"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- Networking ---
  dio: ^5.4.0
  web_socket_channel: ^2.4.0
  http: ^1.2.0

  # --- Firebase ---
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.6

  # --- State Management ---
  provider: ^6.1.2

  # --- Storage ---
  shared_preferences: ^2.3.3

  # --- UI ---
  flutter_svg: ^2.0.10
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  flutter_local_notifications: ^18.0.1

  # --- Utils ---
  intl: ^0.19.0
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  connectivity_plus: ^6.1.1
  package_info_plus: ^8.1.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # assets: add image/icon files here when needed
